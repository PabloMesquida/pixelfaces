import React from "react";
import { LoaderTxt, LoaderContainer } from "./Loader.styles.js";

const Loader = () => {
  return (
    <LoaderTxt>
      <b>Loading.</b>
    </LoaderTxt>
  );
};

export default Loader;
